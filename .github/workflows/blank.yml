name: Test
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: ls
        run: pwd; ls -al
      - name: env
        run: env
      - name: bazel
        run: which bazel; bazel version
      - name: my_script
        run: ./myscript.sh
